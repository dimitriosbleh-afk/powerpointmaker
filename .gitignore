node_modules/
slidetemp/
output/
outputs/
tmp/
.vscode/
.DS_Store
slide-*.jpg
slidetemp_montage.jpg
__pycache__/
*.pyc
.claude/
_workspace/
cranky_notes_workspace/

# Local secrets and credentials
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.p8
id_rsa*
id_dsa*
id_ecdsa*
id_ed25519*
credentials.json
secrets.json
service-account*.json
*service-account*.json
google-credentials*.json
google_oauth_client*.json
client_secret*.json
token*.json
.google/

# Generated extraction/debug artifacts
tmp_*.txt
*_dump.json
*_extract*.txt
scripts/extracted_*.txt
scripts/_*_dump.txt
scripts/_*.md
scripts/ff_l*.txt
scripts/ff_notes/
scripts/notes*.json
scripts/_l_extracts/
scripts/_ochre_dump/
_archive/*_dump.txt

# Auslan sign images (Signbank-derived, internal school teaching use only).
# NEVER commit these - this repo has a public remote and publishing them would
# be redistribution outside both the CC BY-NC-ND licence and the schools
# statutory educational licence. They are rebuilt in minutes by
# scripts/fetch_auslan_signs.py; manifest.json records exactly what to rebuild.
assets/auslan_signs/*.jpg
assets/auslan_signs/*.png
# manifest.json embeds Signbank's dictionary definitions; it regenerates with
# the images, so it stays local too.
assets/auslan_signs/manifest.json

# Purchased teaching references held locally so the Auslan game bank can be
# expanded. Copyright the publisher's; the school's purchased copy puts use
# under the schools statutory educational licence, which covers internal
# teaching and NOT publishing to a public remote. Never commit these.
reference/

# The Auslan game bank reproduces mechanics adapted from those references, with
# page-level citations, so it inherits the same licence: internal school
# teaching only, never redistributed. Its own licensing section says so. It
# stays local and is pasted into a chat from disk, never tracked here.
IMPORTANT/AUSLAN_GAME_BANK.md
assets/persuasive/
